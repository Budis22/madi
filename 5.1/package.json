{
  "name": "smart-parking-spa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:lighthouse:before": "VITE_LIGHTHOUSE_BEFORE=1 vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5173 --strictPort",
    "lighthouse:before": "npm run build:lighthouse:before && npm run preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "pinia": "^2.3.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
